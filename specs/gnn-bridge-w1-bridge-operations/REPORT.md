# W1 report — bridge operations status command

Slice: `specs/gnn-bridge-w1-bridge-operations/` (operations stage of the
GNN bridge program). Date: 2026-09-04. Spec: [README.md](README.md).
All commands run from the repo root
(`projects/outside_of_hum/fep_lean`) on digest
`67601bce1e04` (fep_lean main) / `aa20514c77bb` (GNN main) unless noted.

## Artifacts

| Path | State | Role |
| --- | --- | --- |
| `specs/gnn-bridge-w1-bridge-operations/README.md` | new | Slice spec (opened before code) |
| `specs/gnn-bridge-w1-bridge-operations/bridge_status.py` | new | The four-check status command |
| `specs/gnn-bridge-w1-bridge-operations/syntax-pin.json` | new | Q1 syntax-surface pin (GNN HEAD, both files clean at pin time) |
| `specs/gnn-bridge-w1-bridge-operations/REPORT.md` | new | This file |
| `docs/design/gnn-bridge/README.md` | modified | "Operating the bridge" section (five commands of record) appended |
| `specs/gnn-bridge-p1-finite-spike/gnn-input/FepLeanSymmetricBool.md` | regenerated via `--refresh-digests` | Custody-drift healing after HEAD moves (digest lines only) |

No edits to any accepted slice's script: the P1 emitter is imported via
`importlib` and its `build_document(heads)` invoked; the P3 certifier and
the projection gate are subprocessed.

## Check design notes

- **Freshness classification.** A mismatch is `STALE-CUSTODY
  (digest-only)` iff every line that differs between on-disk and
  regenerated bytes is a provenance digest line (`source_commit:` /
  `pipeline_commit:` prefix); any other differing line classifies as
  `CONTENT-DRIFT`. Both FAIL the gate; remediation differs
  (`--refresh-digests` vs deliberate re-emission).
- **Syntax-surface pin.** Both pinned files were verified clean against
  GNN HEAD (`git status --short -- doc/gnn/gnn_syntax.md
  src/pipeline/step_registry.py` empty) at pin time, so the pin is a
  plain HEAD pin (`gnn_commit: aa20514c77bb...`); the working-tree-pin
  contingency of the plan did not trigger. A moved GNN HEAD with
  byte-identical pinned files is reported as an informational note, not
  a failure — the file digests are the contract.
- The P3 certifier subprocess regenerates `certificates.json` /
  `CERTIFICATES.md` byte-identically when green (accepted P3 custody
  behavior).

## Commands and exit codes

| Command | Exit | Result |
| --- | --- | --- |
| `uv run ruff check specs/gnn-bridge-w1-bridge-operations/` | 0 | All checks passed |
| `uv run mypy specs/gnn-bridge-w1-bridge-operations/bridge_status.py` | 0 | Success: no issues found in 1 source file |
| `uv run python specs/gnn-bridge-w1-bridge-operations/bridge_status.py` (pre-refresh) | 1 | freshness FAIL `STALE-CUSTODY (digest-only)` (expected: heads moved since P1 emission); other 3 PASS — classification exercised live |
| `uv run python specs/gnn-bridge-w1-bridge-operations/bridge_status.py --refresh-digests` | 0 | refreshed `FepLeanSymmetricBool.md` (5591 bytes) |
| `uv run python specs/gnn-bridge-w1-bridge-operations/bridge_status.py` (post-refresh) | 0 | 4/4 PASS |
| `uv run python docs/check_links.py --strict --include-root` | 0 | 47 files, no broken links (after README edits) |
| `uv run python docs/md_hygiene.py --strict` | 0 | 43 files, no hygiene issues |

## Honest boundary

- The status command exercises accepted-slice tooling read-only; it
  proves the bridge's recorded state is self-consistent (emitter bytes,
  certificate gate, pinned syntax surface, canonical→mirror projection),
  nothing more. It is not a semantic re-review of any accepted slice.
- The STALE-CUSTODY classification path was exercised live (heads moved
  since P1); the CONTENT-DRIFT path is implemented and unit-reasoned but
  was not triggered by a real content divergence in this run.
