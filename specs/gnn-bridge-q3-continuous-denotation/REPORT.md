# Q3 evidence record — GNN bridge continuous-family denotation

Status: **complete and verified**. This report is the evidence record for
[README.md](./README.md); commands and exit codes below were executed on
2026-09-04 from the repo root (`projects/outside_of_hum/fep_lean`) unless
noted. Nothing here is committed; the orchestrator owns the commit.

## Artifacts

| Path | State | Role |
| --- | --- | --- |
| `src/fep_lean/formal/gnn_denotation_continuous.lean` | new, 344 lines | Q3 denotation module (namespace `FEP.GnnContinuous`) |
| `src/fep_lean/formal/manifest.py` | modified, +6 lines | Registers `FepSketches.gnn_denotation_continuous` as a FOUNDATION row immediately after the `gnn_denotation` row |
| `lean/FepSketches/gnn_denotation_continuous.lean` | new, generated | Script-owned projection of the canonical module (verified byte-identical via `diff -q`) |
| `specs/gnn-bridge-q3-continuous-denotation/README.md` | new | Frozen-conventions spec (opened before any Lean code) |
| `specs/gnn-bridge-q3-continuous-denotation/REPORT.md` | new | This file |

Shared files this slice touched by design: `manifest.py` carries this slice's
row plus the `gnn_render_statements` row added by the q4statements agent under
the first-mover contract (my row landed first, at manifest mtime 1788546753).
Script-owned doc projections (`docs/formalism-coverage.json`,
`docs/formalism-coverage.md`, `docs/lean-landscape.md`) were regenerated by the
projection tooling when the new modules were registered; this worker ran only
`--check` forms. Remaining `git status` ownership beyond the table above: the
q4 slice owns
`gnn_render_statements.lean` (canonical+projection),
`specs/gnn-bridge-q4-renderer-execution-statements/`, its `manifest.py` row,
and the `tests/test_formal_composition.py` roster-test edit (which registers
BOTH new rows — `FEP.GnnContinuous` and `FEP.GnnRenderStatements` — so the
manifest-roster test covers this slice's registration too). The
`specs/gnn-bridge-p3-certificates/REPORT.md` modification (a P3
README-reconciliation note) predated this session — present in the first
`git status` before this slice touched anything — and is owned by neither
Q3 nor Q4.

## Statements and proof status

All in namespace `FEP.GnnContinuous` unless noted.

| Statement | Kind | Status |
| --- | --- | --- |
| `dimNat`, `dimsNat`, `findDecl`, `declDims`, `parameterizes`, `bindsTerm` | defs (document lookups, mirroring `FEP.GnnDenotation` without importing it) | compiled |
| `continuousFieldVars`, `continuousFieldBindings` | defs (frozen six-variable surface and ontology bindings) | compiled |
| `ContinuousPayload` | payload structure (`fTransition`, `hObservation`, `qProcess`, `rObservation`, `priorMean`, `priorCov`) | compiled |
| `ContinuousConforms` | structure (11 conformance fields: well-formedness, seven literal-dims checks, parameterization, ontology bindings, `priorCov.PosDef`) | compiled |
| `denoteContinuous` | the denotation def (alignment statement 3, prior gauge) | compiled (`noncomputable`) |
| `linearGaussianParameters_ext` | theorem (fieldwise extensionality helper) | proved |
| `diagonal_inv` | theorem (mechanical prior-gauge inversion: diagonal inverse = pointwise inverses) | proved |
| `sdDoc` | exemplar transcription of `GeneralizedNotationNotation/input/gnn_files/continuous/stochastic_dynamics.md` | compiled |
| `sdPayload` | exemplar typed payload, exact rationals (`F = [[1, 1/10], [0, 9/10]]`, `H = [[1, 0], [1, 0]]`, `Q = (1/10)·I`, `R = (1/5)·I`, `prior_mean = (0, 0)`, `prior_cov = diag(1/2, 1)`) | compiled (`noncomputable`) |
| `sdDoc_wellFormed` | theorem | proved by `decide` |
| `sdConforms` | theorem | proved (`refine` + `simp` bullets + `Matrix.PosDef.diagonal`) |
| `stochasticDynamicsParameters` | hand-stated reference carrier instance (precision `diag(2, 1)`, zero center) | compiled (`noncomputable`) |
| `sdDoc_denotation` | theorem — `denoteContinuous sdDoc sdPayload sdConforms = stochasticDynamicsParameters` | proved (`linearGaussianParameters_ext` + `diagonal_inv` + pointwise rational arithmetic) |
| `sdDoc_covariance` | theorem — denoted stationary covariance equals the declared prior covariance | proved |
| `sdDoc_covariance_posDef` | theorem — denoted stationary covariance is positive definite | proved |
| `sdDoc_transitionMean_zero`, `sdDoc_transitionCovariance_zero` | theorems — zero-time Dirac boundary via the carrier's `transitionMean_zero` / `transitionCovariance_zero` | proved |

## Commands and exit codes

| Command | Result |
| --- | --- |
| `cd lean && lake env lean ../src/fep_lean/formal/gnn_denotation_continuous.lean` (canonical-file compile probe, run twice during development) | first run: errors fixed in the canonical module (missing `[Fintype n]` on `diagonal_inv`; `Matrix.isUnit_iff_isUnit_det` used without dot-notation application; carrier theorems needed the `LinearGaussianParameters.` qualifier; two unreachable `norm_num`s); second run: clean, zero errors, zero warnings |
| `cd lean && lake build FepSketches.gnn_denotation_continuous` | exit 0 — `Build completed successfully (8719 jobs)`; zero errors, zero warnings |
| `cd lean && lake build FepSketches` | exit 0 — `Build completed successfully (8764 jobs)`; zero errors, zero warnings |
| `cd lean && lake env lean /tmp/q3-axcheck.lean` (file: `import FepSketches.gnn_denotation_continuous` + `#print axioms` for all nine theorems) | exit 0 — every theorem `depends on axioms: [propext, Classical.choice, Quot.sound]`; no `sorryAx`, no new axioms |
| `uv run python scripts/_maint_build_formal_modules.py --check` | exit 0 — `OK: formal Lean workspace projections are current` |
| `uv run python scripts/_maint_build_fep_all_lean.py --check` | exit 0 — `OK: lean/FepSketches/fep_all.lean is current` |
| `uv run python scripts/build_formalism_coverage.py --check` | exit 0 |
| `uv run python scripts/_maint_build_lean_landscape.py --check` | exit 0 |
| `uv run python docs/check_links.py --strict --include-root` | exit 0 — 47 files, no broken links |
| `uv run python docs/md_hygiene.py --strict` | exit 0 — 43 files, no hygiene issues |
| `uv run pytest tests/test_formal_composition.py tests/test_native_evidence.py -q` | exit 0 — 43 passed in 23.81s |

Mirror-integrity checks (guarding the known canonical-vs-mirror divergence
trap recorded from the Q2 recovery): `diff -q` of
`src/fep_lean/formal/gnn_denotation_continuous.lean` against
`lean/FepSketches/gnn_denotation_continuous.lean` — identical; same for the q4
module. A direct from-source compile of the q4 mirror
(`lake env lean FepSketches/gnn_render_statements.lean`) was also run to
confirm the full-build success was not served from stale artifacts.

## Integration account (matters for the next reviewer)

This slice and the q4 slice shared `manifest.py` and the projection gate. Per
the first-mover contract, this worker's `gnn_denotation_continuous` manifest
row landed first; the projection regeneration then briefly failed on
`formal_resource_manifest_drift` because the q4 module file was present on
disk before its row. The q4 agent registered its own row and ran the
projection (which also projected this slice's module). During that window a
direct compile probe captured a transient broken state of the q4 module; after
the file stabilized, this worker re-verified the final shared state: canonical
and mirror byte-identical for both modules, from-source compile clean, full
`lake build FepSketches` exit 0, and both `--check` gates green. No hand edits
to `lean/FepSketches/*` were made by this worker; every mirror arrived through
the projection scripts.

## Honest boundary (evidence firewall)

- What the Lean build proves: the Q3 module type-checks in the pinned
  workspace with zero errors, zero warnings, no `sorry`, and no axioms beyond
  the three standard Mathlib axioms; the fixed-exemplar denotation statement
  and its four corollaries hold on the `Fin 2` axis under the frozen
  conventions.
- What it does not prove: anything about GNN pipeline behavior (bridge
  contract section 7). The payload-string → typed-table step is applied by
  hand in `sdPayload`, not parsed. The `F`/`H`/`Q`/`R` payload values are
  recorded but not consumed — the carrier has no observation fields, and
  identifying the document's one-step decimal dynamics with the carrier's
  `exp(-t·precision)` law is the P4 no-go (transcendental evolution entries;
  the exemplar's Euler `F` is not even symmetric); a dynamics-gauge slice is
  the recorded follow-up. The axis enumeration (slot `i` ↔ `Fin i`) is the
  isomorphism-class caveat: the swapped enumeration transports the prior
  covariance to `diag(1, 1/2)` and the precision to `diag(1, 2)`, a different
  instance. `type=float` exactness is explicitly not claimed over the exact
  rational payload (README convention 5).
- The hand-stated reference instance `stochasticDynamicsParameters` lives in
  the same module as the denotation; the acceptance statement's content is
  that the frozen prior-gauge convention mechanically recovers its
  hand-derived precision `diag(2, 1)` and zero center from the document's
  exact literals (`Matrix.inv` of `diag(1/2, 1)` vs the hand-stated
  diagonal), not that two independent codebases were compared.
- No new catalogue topics, relations, atlas claims, or `fep-NNN` identifiers;
  `composed.lean` and `fep_all.lean` are unchanged in content and do not list
  these modules.
- The tree is intentionally left uncommitted for the orchestrator.

- GitNexus impact analysis / `detect_changes()` were unavailable in this
  session; per the repo's availability policy the reduced confidence is
  flagged here, and the change-scope evidence is the fresh `lake build` plus
  the gate battery above (this slice's change surface is exactly the five
  artifact paths in the table).
