# Q2 evidence record — GNN bridge discrete-family denotation

Status: **complete and verified**. This report is the evidence record for
[README.md](./README.md); commands and exit codes below were executed on
2026-09-04 from the repo root (`projects/outside_of_hum/fep_lean`) unless
noted. Nothing here is committed; the orchestrator owns the commit.

## Recovery account (matters for the next reviewer)

The parked work in `/tmp/q2-parking/` was wiped by the host reboot; the
uncommitted tree was the only copy. Audit revealed a divergence: the
canonical module `src/fep_lean/formal/gnn_denotation.lean` still held the
pre-fix Q2 code, while the predecessor's fixes existed only in the
generated mirror `lean/FepSketches/gnn_denotation.lean` (the first rebuild
compiled the mirror and left only two residual error spots). The
projection regeneration then overwrote that mirror from canonical source,
which surfaced all five known error classes at once. Every fix was
re-derived from the resume note's error list and re-applied to the
canonical source; mirrors were regenerated by the build scripts only
(hand edits under `lean/FepSketches/` remain forbidden).

Fixes re-applied:

- `dimsNat` rewritten as structural recursion with the recursive call
  outside match-scrutinee position (`Option.bind`/`Option.map`), removing
  the fold type mismatch at the old line ~89.
- `symBoolPayload` marked `noncomputable` (codegen depends on
  `Real.instDivInvMonoid`).
- The five declaration-dimension bullets gained `dimsNat, dimNat` simp
  arguments; residual `dimsNat [lit 2, …] = some [2, …]` goals close.
- Membership destructuring for `paramFields`/`ontologyFields` flattened
  with `simp only […, List.mem_cons, List.not_mem_nil, or_false] at hv`
  before `rcases` — the same idiom as
  `src/fep_lean/formal/variational_duality.lean:584-587` — since
  `rcases` cannot case a literal list tail.
- The `eHabit` payload bullet case-splits on `Bool` before `norm_num`.
- The denotation simp sets gained `symmetricBoolModel` (with
  `fairBoolLaw`/`fairBoolKernel`/`trueBiasedPolicyPrior`), which both
  closes the five mass-equality goals and clears the four
  unused-simp-arg warnings.
- Linter-clean pass: dead `decide` branches and idle `norm_num` closers
  removed; `unnecessarySeqFocus` chains restructured as per-case proofs.

## Artifacts

| Path | State | Role |
| --- | --- | --- |
| `src/fep_lean/formal/gnn_document.lean` | new, 653 lines | Accepted Q1 AST module, unchanged; reused via extractors |
| `src/fep_lean/formal/gnn_denotation.lean` | new, 360 lines | Q2 denotation module (namespace `FEP.GnnDenotation`) |
| `src/fep_lean/formal/manifest.py` | modified, +12 lines | Registers `FepSketches.gnn_document` and `FepSketches.gnn_denotation` as FOUNDATION rows after the `posterior_convergence` row |
| `lean/FepSketches/gnn_document.lean`, `lean/FepSketches/gnn_denotation.lean` | new, generated | Projections of the two canonical modules (byte-identical, script-owned) |
| `specs/gnn-bridge-q2-discrete-denotation/README.md` | modified, 1 line | Status date updated to the verification date |
| `specs/gnn-bridge-q2-discrete-denotation/REPORT.md` | new | This file |

No other file changed; `git status --porcelain` at verification time shows
exactly the rows above.

## Statements and proof status

All in namespace `FEP.GnnDenotation` unless noted.

| Statement | Kind | Status |
| --- | --- | --- |
| `dimNat`, `dimsNat`, `findDecl`, `declDims`, `parameterizes`, `bindsTerm` | defs (document lookups) | compiled |
| `DiscretePayload`, `VectorLaw`, `KernelPayload`, `LikelihoodPayload` | payload types / normalization props | compiled |
| `DiscreteConforms` | structure (13 conformance fields) | compiled |
| `lawOfMass`, `kernelOfMass` | carrier construction helpers | compiled |
| `denoteDiscrete` | the denotation def (alignment statement 1) | compiled |
| `symBoolDoc` | P1 exemplar transcribed to the Q1 AST | compiled |
| `symBoolPayload` | exemplar typed payload, exact literals | compiled (`noncomputable`) |
| `symBoolDoc_wellFormed` | theorem | proved by `decide` |
| `symBoolConforms` | theorem | proved (refine + simp/norm_num + flattened rcases) |
| `generativeModel_ext` | theorem (fieldwise extensionality helper) | proved |
| `symBoolDoc_denotation` | theorem — `denoteDiscrete symBoolDoc symBoolPayload symBoolConforms = symmetricBoolModel trueBiasedPolicyPrior` | proved (five mass equalities via simp) |
| `symBoolDoc_predictedState`, `symBoolDoc_predictedOutcome`, `symBoolDoc_fullSupport` | theorems (denoted model drops into carrier theorems) | proved via `rw [symBoolDoc_denotation]` + existing carrier theorems |

## Commands and exit codes

| Command | Result |
| --- | --- |
| `cd lean && lake build FepSketches.gnn_document FepSketches.gnn_denotation` | exit 0 — `✔ [8710/8710] Built FepSketches.gnn_denotation (55s)`, `Build completed successfully (8710 jobs)`; zero errors, zero warnings |
| `cd lean && lake build FepSketches` | exit 0 — `Build completed successfully (8762 jobs)` |
| `cd lean && lake env lean /tmp/q2-axcheck.lean` | exit 0 — all six theorems `depends on axioms: [propext, Classical.choice, Quot.sound]`; no `sorryAx`, no new axioms |
| `uv run python scripts/_maint_build_formal_modules.py --check` | exit 0 |
| `uv run python scripts/_maint_build_fep_all_lean.py --check` | exit 0 |
| `uv run python scripts/build_formalism_coverage.py --check` | exit 0 |
| `uv run python scripts/_maint_build_lean_landscape.py --check` | exit 0 |
| `uv run python docs/check_links.py --strict --include-root` | exit 0 — 47 files, no broken links |
| `uv run python docs/md_hygiene.py --strict` | exit 0 — 43 files, no hygiene issues |
| `uv run pytest tests/test_formal_composition.py tests/test_native_evidence.py -q` | exit 0 — 43 passed in 18.85s |

## Honest boundary (evidence firewall)

- What the Lean build proves: the Q2 module type-checks in the pinned
  workspace with zero errors, zero warnings, no `sorry`, and no axioms
  beyond the three standard Mathlib axioms; the fixed-exemplar
  isomorphism-class statement and its three corollaries hold on the
  Boolean carrier.
- What it does not prove: anything about GNN pipeline behavior (bridge
  contract section 7). The payload-string → typed-table step is applied
  by hand in `symBoolPayload`, not parsed; the brace-block/decimal parser
  remains an explicit follow-up recorded in the README. The index
  enumeration is frozen as `Bool: false, true`; a different enumeration
  yields the transported model (isomorphism-class caveat). `F[π]`/`G[π]`
  reference-dimension reading stays deferred.
- No new catalogue topics, relations, atlas claims, or `fep-NNN`
  identifiers; `composed.lean` and `fep_all.lean` are unchanged in
  content and do not list these modules.
- The tree is intentionally left uncommitted for the orchestrator.

- GitNexus impact analysis / `detect_changes()` were unavailable in this
  session; per the repo's availability policy the reduced confidence is
  flagged here, and the change-scope evidence is the fresh `lake build`
  plus the gate battery above (the change surface is exactly the seven
  committed paths).
