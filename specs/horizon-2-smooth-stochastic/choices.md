# Horizon 2 choices ledger

## Sound

### Accept H2.0 as complete with row-level boundaries

- **Choice:** Seal H2.0 with 25 `go`, 13 `optional_no_go`, three
  `blocking_no_go`, and one `upstream_required` row instead of treating one
  failed seam as either total H2 failure or permission to omit it.
- **Reason:** Scalar Gaussian, calculus, native-kernel, exact embedding,
  multivariate-kernel, and finite-grid prerequisites compiled, while dynamic
  `K_star` covariance, Gaussian precision conditioning, and the selected
  closed-form native filter posterior did not.
- **Verdict:** Sound, high confidence. The DAG opens only row-green slices and
  keeps every affected terminal edge closed.

### Derive one nonvacuous scalar native-KL witness

- **Choice:** Prove the oriented unit-variance, unit-shift identity directly
  from Radon--Nikodym derivatives and integrable log likelihood ratio, rather
  than greening the row with `klDiv_self` or a totalized real projection.
- **Reason:** The concrete nonzero-shift witness establishes finiteness,
  orientation, and the (1/2) value without pretending H2.0 already owns the
  general Gaussian theorem.
- **Verdict:** Sound, high confidence.

### Construct the multivariate state kernel without a convenience theorem

- **Choice:** Build the selected state-dependent multivariate Gaussian Markov
  kernel from native `Kernel.compProd` and `Kernel.map`, while keeping the
  separate time-dependent covariance theorem blocked.
- **Reason:** No single covariance-parameter measurability theorem was located,
  but the actual kernel construction compiled. Kernel measurability does not
  imply that the intended `K_star` transition covariance is PSD.
- **Verdict:** Sound, high confidence.

### Keep finite-dimensional Brownian support optional

- **Choice:** Record the finite-dimensional projective-family regression as
  `optional_no_go` even though it compiles.
- **Reason:** It does not yet shorten the direct OU transition proof and it
  supplies no continuous-path, Itô, or SDE result.
- **Verdict:** Sound, high confidence.

### Bind readiness through one canonical receipt

- **Choice:** Make every row reference one canonical JSON receipt whose digest
  binds the exact test, validator, probe, and toolchain sources, while a
  canonical scientific-contract digest binds the ordered row payloads both in
  the matrix and inside that receipt. Per-row receipt pointers are excluded
  from the row digest solely to avoid a circular hash dependency.
- **Reason:** Duplicating 42 independent run receipts would add drift without
  increasing evidence: the same serialized suite validates all rows and the
  tamper tests enforce the boundary.
- **Verdict:** Sound, high confidence.

### Capture the empty H2 resource roster without freezing future phases

- **Choice:** Require the H2.0 acceptance receipt to contain an exact empty
  `formal_resources_at_capture` roster, but stop checking the live manifest for
  absence after the matrix reaches `complete_with_boundaries`.
- **Reason:** Resource absence is an H2.0 entry/exit condition. Treating it as a
  permanent invariant would make the readiness validator reject the first
  legitimate H2.1a module and turn accepted evidence into a temporal trap.
- **Verdict:** Sound, high confidence. Pending H2.0 evidence still fails closed,
  while completed source-bound evidence survives later additive modules.

### Source-bind the latest-stable toolchain decision

- **Choice:** Preserve the exact official `git ls-remote` tag queries and
  returned revisions in canonical `pin_evidence.json`, and include that file in
  the acceptance source roster owned by `pin_identity`.
- **Reason:** Local lockfiles prove which pin is installed, not that it was the
  latest matching stable Lean/Mathlib pair when H2.0 exited. The query receipt
  records both the selected `v4.33.1` pair and the absence of stable `v4.34.0`
  despite its release-candidate tags.
- **Verdict:** Sound, high confidence. The validator cross-checks the stable pair
  against the local toolchain and fails on unsafe, noncanonical, or mismatched
  evidence.

### Split by proof seam rather than initial file plan

- **Choice:** Separate scalar OU, generic linear-Gaussian algebra, exact
  four-coordinate construction, and conditioning/precision; separately split
  filter, control, and finite-grid path laws.
- **Reason:** Each has a distinct carrier, fatal premise, and no-go verdict.
  One large file would allow a green algebraic endpoint to hide a failed
  measurability, conditioning, or control bridge.
- **Verdict:** Sound, high confidence.

### Keep embedded-kernel functoriality with its existing owner

- **Choice:** Prove identity and composition preservation beside
  `FEP.NativeBlanket.embeddedKernel`, then consume them from H2.4.
- **Reason:** The embedding is the concept owner; duplicating its laws in the
  semigroup module would create a second translation vocabulary.
- **Verdict:** Sound, high confidence.

### Reuse predictive embedding to prove kernel composition

- **Choice:** Derive `embeddedKernel_comp` rowwise from the existing
  `embeddedPredictive_eq_comp`, after identifying a composed finite-kernel row
  with prediction of the earlier row through the later kernel.
- **Reason:** Recomputing the native lintegral would duplicate the established
  finite-to-native predictive bridge and widen the maintenance surface.
- **Verdict:** Sound, high confidence. The refactored proof is warning-free,
  and a noncommutative Boolean test pins chronological orientation.

### Make coordinate labels theorem-visible

- **Choice:** In natural coordinate `eta = mu / variance`, retain
  log-partition Hessian, Fisher information, and covariance equal to the fixed
  variance; in mean coordinate, retain reciprocal-variance Fisher information.
- **Reason:** “Fisher equals covariance” is coordinate-dependent and is false
  on the selected mean-coordinate surface.
- **Verdict:** Sound, high confidence.

### Require the four-coordinate carrier before H2 exit but keep it separate

- **Choice:** H2.7 has one scalar vertical theorem and a distinct formal
  four-coordinate export. H3 may inspect but never construct that export.
- **Reason:** Forcing the scalar theorem to masquerade as a blanket carrier or
  postponing `Fin 4` to H3 would violate both carrier identity and
  preregistration.
- **Verdict:** Sound, high confidence.

### Treat conditioning as its own fatal gate

- **Choice:** Precision zeros and inverse-covariance arithmetic do not count as
  conditional independence. A native conditional law or proved factorization
  is required in its own owner.
- **Reason:** Pinned source inspection found algebraic Gaussian primitives but
  no ready-made multivariate conditioning theorem.
- **Verdict:** Sound, high confidence; implementation feasibility remains an
  H2.0 decision.

### Remove mandatory generator and weak Fokker--Planck language

- **Choice:** Scalar OU acceptance requires an explicit transition kernel,
  semigroup, invariant law, moments, and weak convergence. Generator or weak
  forward identities are optional only after an explicit test-function-class
  probe.
- **Reason:** A transition-law construction is not an SDE solution, and a
  formal derivative slogan is not a Fokker--Planck theorem.
- **Verdict:** Sound, high confidence.

### Keep H2 status spec-owned until theorem acceptance

- **Choice:** Do not add speculative H2 nodes to the topic, novelty, relation,
  or capability owners.
- **Reason:** Those schemas describe accepted release surfaces; an active spec
  already owns prospective package state.
- **Verdict:** Sound, high confidence.

### Keep one fixed-positive-variance H2.1 carrier

- **Choice:** Store only one strictly positive variance in
  `FixedVarianceGaussian` and keep the mean as an explicit argument of the
  canonical law and density.
- **Reason:** Normalization, support, absolute continuity, and KL remain
  derived properties. The carrier cannot hide a desired theorem as a field or
  silently admit Mathlib's singular zero-variance branch.
- **Verdict:** Sound, high confidence.

### Prove the general oriented native Gaussian KL formula directly

- **Choice:** Generalize the H2.0 unit-shift spike to arbitrary source and
  reference means at the same positive variance, deriving the density ratio,
  affine log-likelihood ratio, integrability, and source expectation in the
  native `ENNReal` theorem.
- **Reason:** This preserves source-to-reference orientation and the extended
  codomain without routing through the repository's totalized finite KL. The
  zero and strictly positive branches are then corollaries of one maintained
  theorem.
- **Verdict:** Sound, high confidence.

### Keep H2.1a independent of finite and coordinate owners

- **Choice:** Import only the pinned Gaussian and native-KL Mathlib owners in
  H2.1a. Do not retain unused project imports or introduce natural/mean
  coordinates before H2.1b.
- **Reason:** The measure theorem is already complete on its native carrier;
  importing finite exponential-family or information-geometry modules would
  create name-level coupling rather than a proved bridge.
- **Verdict:** Sound, high confidence.

### Derive both scores from the actual Gaussian density ratio

- **Choice:** Rewrite the same-variance Gaussian density ratio against the
  zero-mean base law to its quadratic natural/mean-coordinate form, then prove
  each score as that actual log-ratio derivative before proving centering.
- **Reason:** A locally named centered function is not automatically a score.
  The density-ratio equality binds the coordinate calculus to the accepted
  H2.1a law without importing the finite `ScoreModel` carrier.
- **Verdict:** Sound, high confidence.

### Keep Fisher and covariance coordinate-qualified

- **Choice:** Prove literal `cov[X,X]` equals natural-coordinate Fisher and the
  fixed variance, while mean-coordinate Fisher equals reciprocal variance and
  the natural metric times two reciprocal-variance Jacobian factors.
- **Reason:** This is the correct coordinate transformation law. The test
  allowlists only `naturalFisher_eq_covariance`, preventing an unqualified or
  mean-coordinate covariance equality from entering later.
- **Verdict:** Sound, high confidence.

### Narrow provisional readiness dependencies at implementation time

- **Choice:** Use only the H2.0 coordinate-duality route and direct
  `Deriv.Mul` owner for H2.1b. The proof first rewrites the log-density ratio to
  a quadratic, so finite-sum and direct exp/log derivative probes remain green
  readiness ingredients but are not live source dependencies.
- **Reason:** The H2.0 receipt records the preimplementation probe decision;
  the active slice owns the final source-true import and dependency tuple.
  Rehashing historical H2.0 evidence to retain unused edges would reduce
  rather than improve provenance.
- **Verdict:** Sound, high confidence.

### Export fixed-variance law injectivity for the posterior lane

- **Choice:** In addition to natural-to-mean injectivity, prove that the
  accepted fixed-variance Gaussian law is injective in its mean by reusing
  Mathlib's `gaussianReal_ext_iff`.
- **Reason:** This is a derived same-carrier identifiability fact needed by the
  eligible H2.3 lane; it adds no posterior-consistency or multivariate claim.
- **Verdict:** Sound, high confidence.

## Planning provenance

The canonical plan synthesizes independent fewest-slices, risk-first, and
seam-quality Sol drafts. A read-only Claude Opus draft was attempted to satisfy
model-family diversification, but local OAuth had expired; no credential or
configuration mutation was authorized. No design decision relies on the
failed invocation.

### Keep H2.2a as scalar coordinate algebra

- **Choice:** Prove the fixed-variance Gaussian metric, pullback, duality
  product rule, affine paths, and KL/Bregman identity directly on the accepted
  natural and mean coordinate maps. Introduce no local manifold, tangent,
  connection, Christoffel, or curvature hierarchy.
- **Reason:** The selected one-dimensional family has constant metric
  coefficients and an affine natural-to-mean map. Bundling a second geometry
  vocabulary would add owners without strengthening the terminal theorem.
- **Verdict:** Sound, high confidence. The exact coordinate equations compile
  and the forbidden-hierarchy contract is mechanical.

### Make the redundant-coordinate boundary a genuine pullback

- **Choice:** Bundle the duplicated-mean Jacobian as a continuous linear map,
  prove it is the Fréchet derivative of the parameterization, and evaluate the
  mean Fisher metric at the mapped parameter before proving a nonzero null
  tangent.
- **Reason:** A function merely named `Jacobian` beside an unrelated scalar
  metric point would not establish rank deficiency. The derivative and
  mapped-point equalities make the countermodel scientific rather than
  nominal.
- **Verdict:** Sound, high confidence. Independent preflight identified this
  gap before acceptance; the repaired theorem and axiom probes are green.

### State flat duality only at the strength proved

- **Choice:** Expose the same-coordinate derivative of the natural/mean field
  pairing as the sum of two explicit metric pairings, with `HasDerivAt`
  premises. Describe it as a zero-coefficient scalar-chart product rule, not a
  general dual-connection construction.
- **Reason:** Fixed variance makes both metric coefficients constant. The
  equation is sufficient for the selected terminal geometry clause but does
  not justify global connection, torsion, curvature, or dual-flatness claims.
- **Verdict:** Sound, high confidence.

### Leave H2.2b optional and unopened

- **Choice:** Do not bundle the accepted equations into Mathlib's manifold API
  unless a future red-first usefulness test demonstrates net proof
  compression and no parallel ownership.
- **Reason:** H2.2a already supplies the required theorem edge. Optional
  packaging is not evidence and must not delay H2.3/H2.4.
- **Verdict:** Sound, high confidence.

### Certify an already-Markov native family instead of storing stochasticity

- **Choice:** Parameterize `NativeKernelSemigroup` by a family whose slices
  already carry `IsMarkovKernel`, and store only the zero and ordered addition
  laws. Keep invariant and reversible laws as external predicates.
- **Reason:** Mathlib's raw `Kernel` type does not imply stochasticity, but
  storing stochasticity, invariance, or generator existence as semigroup
  conclusions would make the target theorem true by record construction. The
  typeclass boundary supplies lawful kernels while the structure certifies
  only the missing semigroup algebra.
- **Verdict:** Sound, high confidence. The exact H1 embedded lift constructs
  the certificate and all public results compile with standard axioms.

### Prove KL monotonicity between arbitrary certified times

- **Choice:** State native KL contraction from `earlier` to
  `earlier + increment`, using the addition law, `Measure.comp_assoc`, and
  kernel DPI. Derive the invariant-reference form by rewriting both reference
  slices with the same invariant law.
- **Reason:** A one-slice inequality against time zero is data processing, but
  it does not by itself expose the promised nonincreasing-in-time theorem.
  The strengthened statement is the smallest exact semigroup consequence.
- **Verdict:** Sound, high confidence. Independent review found the weaker
  first draft and accepted the strengthened orientation.

### Lift only certified H1 semigroups

- **Choice:** Lift the H1 certified finite semigroup and Boolean hold/refresh
  action model exactly, while retaining the directed three-cycle solely as a
  nonreversible generator/current regression.
- **Reason:** H1 never constructed a certified semigroup for the directed
  cycle. Calling it an H2.4b instance would store or assume the missing
  transition family. The two-state and blanket refresh owners already provide
  the lawful semigroups required by this slice.
- **Verdict:** Sound, high confidence. The design and tests now mechanically
  reject the false three-cycle instance claim.

### Preserve action, time, carrier, and transition by literal equality

- **Choice:** Define the native action family as `embeddedKernel` of the exact
  H1 semigroup slice, lift the H1 real sample time into `ℝ≥0`, and prove the
  sampled native kernel is literally the embedded H1 sampled kernel.
- **Reason:** A reassociated blanket product, equinumerous state space,
  translation alias, or second action-transition field would not establish
  reuse of the accepted H1 carrier. Literal equality closes every such gap.
- **Verdict:** Sound, high confidence. The 16-state hold and refresh endpoints
  reduce to native identity and the embedded positive refresh kernel and are
  provably distinct.

### Store one OU variance-rate parameterization

- **Choice:** Store the positive diffusion variance rate `q` and state
  explicitly that `q = sigma^2` under diffusion-amplitude notation. Do not
  store both `q` and `sigma` or an equality certificate between them.
- **Reason:** The transition variance and stationary variance depend on the
  variance rate. Duplicating parameterizations would introduce a consistency
  field instead of mathematics and would blur the exact H2.5b specialization.
- **Verdict:** Sound, high confidence. The accepted formulas use
  `q * (1 - exp (-2*a*t)) / (2*a)` and `q / (2*a)` consistently.

### Make the OU zero-time boundary theorem-visible

- **Choice:** Define one transition family with an explicit zero-time Dirac
  branch and a positive-time Gaussian branch; prove both boundaries and exact
  equality of the positive branch to H2.1's owner.
- **Reason:** Gaussian measures with zero variance are Dirac in Mathlib, but
  silently treating the identity slice as a nondegenerate Gaussian would erase
  the support boundary used by density and KL arguments.
- **Verdict:** Sound, high confidence. Independent review confirmed the split
  and the exact H2.1 bridge.

### Derive Chapman--Kolmogorov from affine Gaussian bind

- **Choice:** Prove one private affine-Gaussian bind theorem, use it to derive
  chronological `right comp left` Chapman--Kolmogorov, and package that exact
  family through H2.4. Do not store composition, invariance, or covariance as
  certificate fields.
- **Reason:** The private lemma owns the analytic calculation once; the public
  semigroup and invariant-law theorems then reuse it without a parallel
  integration vocabulary or circular certificate.
- **Verdict:** Sound, high confidence. The source and independent review agree
  with Mathlib's kernel-composition orientation.

### Require the full continuous-time weak limit

- **Choice:** Prove convergence over the full `NNReal` at-top filter by the
  pinned sequential characterization and Levy convergence, then expose only a
  real bounded-continuous expectation corollary.
- **Reason:** Integer-time convergence would not establish the promised
  semigroup limit. Extending the corollary to bounded measurable or unbounded
  observables would require additional convergence or integrability theorems.
- **Verdict:** Sound, high confidence. Fresh review confirmed both the full-time
  quantification and the deliberately narrow observable class.

### Reuse H2.4 for invariant-reference KL monotonicity

- **Choice:** Apply the accepted H2.4 native DPI theorem to the OU semigroup and
  rewrite the reference by the independently derived invariant law. Do not add
  a local KL contraction proof or call the result thermodynamic free energy.
- **Reason:** H2.4 already owns the general semigroup consequence. Reuse keeps
  KL orientation and time ordering centralized and avoids a second dissipation
  vocabulary.
- **Verdict:** Sound, high confidence. The theorem compares evolved actual law
  to the stationary reference at earlier and later certified times.

### Repair readiness through append-only R0 decisions

- **Choice:** Keep the H2.0 matrix and acceptance receipt immutable. Repair its
  two now-actionable blocking rows through source-bound H2.5b-R0 and H2.6a-R0
  spike/test/decision records before either maintained owner opens.
- **Reason:** The H2.0 receipt truthfully binds an earlier tree with no H2
  resources and failed target proofs. Recapturing it after accepted H2 modules
  exist would rewrite history; silently treating new source as row-green would
  be fail-open.
- **Verdict:** Sound, high confidence. The DAG now represents both proof gates
  explicitly and preserves the original no-go evidence.

### Prove the actual dynamic covariance before H2.5b

- **Choice:** H2.5b-R0 must derive zero, PSD, positive-time PD, and
  chronological addition for
  `Sigma - exp (-t*P) * Sigma * (exp (-t*P))ᵀ` from raw symmetric
  positive-definite precision. An arbitrary diagonal PSD matrix is not a
  repair.
- **Reason:** The transition kernel, Chapman--Kolmogorov law, and multivariate
  Gaussian validity all depend on that exact covariance. Storing positivity or
  proving only the fixed `Fin 4` numeric witness would make the generic owner
  circular or falsely broad.
- **Verdict:** Sound, high confidence. Failure remains an explicit continuous
  branch no-go and does not invalidate accepted scalar work.

### Treat Mathlib posterior equality as evidence-almost-everywhere

- **Choice:** H2.6a-R0 proves Gaussian density factorization, joint-law
  equality, and `=ᵐ[evidenceLaw]` equality to Mathlib's native posterior.
  Pointwise equality is mechanically rejected.
- **Reason:** The native posterior is a disintegration and is unique only
  almost everywhere under the evidence law. A closed Gaussian formula may
  choose a pointwise version, but Mathlib does not identify arbitrary
  off-support values.
- **Verdict:** Sound, high confidence. The selected nondegenerate Gaussian
  model has positive evidence density everywhere, while still assigning zero
  mass to each singleton.

### Distinguish evidence density from atomic evidence

- **Choice:** State `0 < evidenceDensity y` and density nonvanishing, never
  `0 < evidenceLaw {y}` for a continuous Gaussian observation.
- **Reason:** Every singleton has zero Gaussian probability. Confusing density
  with mass would create a formally and scientifically false evidence premise.
- **Verdict:** Sound, high confidence. The R0 contract pins the distinction and
  the downstream filter reuses it.

### Keep H2.6c on native real-valued finite grids

- **Choice:** Build H2.6c with Mathlib `Kernel.partialTraj` on
  `Π i : Iic n, ℝ` and the exact H2.5a OU transition. Do not import the
  project's finite `path_thermodynamics` carrier.
- **Reason:** `FinitePathProtocol` requires a finite path type and point-mass
  `FiniteLaw`; a real-valued Gaussian grid is not an instance of that carrier.
  Sharing the phrase “path law” is not a type-preserving bridge.
- **Verdict:** Sound, high confidence. H2.6c remains a native finite-grid
  composition and H1 finite thermodynamics remains an independent owner.

### Reject descending finite-grid timestamps at the carrier boundary

- **Choice:** Make every H2.6c path API consume a `TimeGrid` with a monotone
  `ℕ → ℝ≥0` timestamp function. Permit repeated timestamps as explicit
  zero-duration identity transitions, but do not accept a raw timestamp
  function.
- **Reason:** Subtraction on `ℝ≥0` truncates a negative difference to zero.
  Without the monotonicity field, a descending schedule would be silently
  repaired into a deterministic repeated coordinate and could change support,
  reversal, and KL behavior.
- **Verdict:** Sound, high confidence. The red-to-green contract rejects the
  raw function signature and the maintained API makes malformed backward time
  unrepresentable.

### Name coordinate reversal without asserting reverse dynamics

- **Choice:** Define `reverseAlignedGridLaw` as the forward law mapped through
  a measurable coordinate-reversal involution. Do not call it a reverse OU
  process or infer detailed balance from H2.5a invariance.
- **Reason:** Stationary marginals do not establish path reversibility. The map
  is mathematically exact and sufficient as the explicitly aligned KL
  reference, while preserving the missing reverse-dynamics boundary.
- **Verdict:** Sound, high confidence.

### Make both native path-KL failure modes explicit

- **Choice:** Orient KL as `klDiv forward reverseAligned`; derive its real
  expected-log-ratio form only under forward absolute continuity and
  integrability of the forward log ratio. Prove `∞` separately when either
  condition fails.
- **Reason:** Deterministic starts or repeated grid times can create singular
  support, and Mathlib's real logarithm and `ENNReal.toReal` are totalized.
  Hiding either premise could turn an infinite divergence into a misleading
  finite zero.
- **Verdict:** Sound, high confidence. The substantive nonnegativity theorem is
  about the real expected log ratio, not the tautology `0 ≤ klDiv`.

### Repair transition covariance generically before the Fin4 export

- **Choice:** Prove H2.5b-R0 for an arbitrary finite axis using the spectral
  decomposition of a raw symmetric positive-definite precision matrix. Use the
  fixed Fin4 matrix only as a consumer, never as the proof carrier.
- **Reason:** A numeric Fin4 diagonalization would not establish the reusable
  H2.5b covariance law, while a new general functional-calculus library would
  exceed the bounded gate. The pinned unitary spectral API closes the exact
  middle ground.
- **Verdict:** Sound, high confidence. The generic warning-free proof derives
  zero, PSD, positive-time PD, and addition; the Fin4 regression consumes it
  without numeric eigenvalue lemmas.

### Derive covariance and positivity instead of storing certificates

- **Choice:** Store no H2.5b-R0 structure. Define covariance as `precision⁻¹`,
  evolution by the matrix exponential, and transition covariance as
  `Σ - Eₜ Σ Eₜᵀ`; take only `precision.PosDef` as the raw theorem premise.
- **Reason:** A field asserting PSD, PD, covariance, or Markovness would encode
  the desired conclusion rather than prove the dynamic law from the model.
- **Verdict:** Sound, high confidence. Every acceptance property is a theorem,
  and the later/right evolution transports the earlier/left covariance in the
  same chronology as H2.4b/H2.5a.

### Preserve the historical covariance no-go with an append-only repair

- **Choice:** Leave H2.0's `transition_covariance_psd` row and acceptance
  receipt unchanged. Record the successful source-bound proof in the versioned
  H2.5b-R0 repair receipt and open only H2.5b implementation.
- **Reason:** The original readiness probe proved only an arbitrary diagonal
  surrogate. Rewriting it after the fact would erase a useful evidence boundary
  and falsely imply that H2.0 had proved the actual dynamic covariance.
- **Verdict:** Sound, high confidence. The repair is independently reviewed,
  hash-bound, and does not open H2.5c/d, H2.7, or H3.

### Derive the native filter joint before invoking posterior uniqueness

- **Choice:** In H2.6a-R0, derive the real Gaussian PDF factorization first,
  lift the generative and candidate joints through exact `withDensity`
  equalities, and identify the actual evidence law by marginalization before
  applying Mathlib's posterior uniqueness theorem.
- **Reason:** A closed Gaussian formula is not evidence that it is the
  posterior for the maintained OU/observation joint. Marginal identification
  closes that ownership gap and keeps the final equality at its correct
  evidence-almost-everywhere scope.
- **Verdict:** Sound, high confidence. The full route compiles warning-free,
  uses standard axioms only, and received independent probability approval.

### Keep H2.6a-R0 inputs primitive and posterior quantities derived

- **Choice:** Store only accepted OU dynamics, a nonnegative step duration,
  and strictly positive Gaussian observation noise. Derive prediction
  variance, innovation variance, gain, evidence, posterior mean, posterior
  variance, and the closed posterior kernel.
- **Reason:** Storing any of those conclusions would make the readiness gate a
  certificate wrapper rather than a proof that the selected model has the
  claimed native posterior.
- **Verdict:** Sound, high confidence. The spike has one Gaussian owner and one
  OU owner and excludes zero-noise or totalized zero-denominator updates.

### Preserve the historical posterior no-go with an append-only repair

- **Choice:** Leave H2.0's `native_filter_posterior` row and acceptance receipt
  unchanged. Record the successful selected-model proof in the versioned
  H2.6a-R0 repair receipt and open only H2.6a implementation.
- **Reason:** H2.0 established API availability on an identity posterior, not
  the scalar Gaussian density/joint-law argument now required. Rewriting that
  receipt would erase the original evidence boundary and overstate what the
  readiness pass proved.
- **Verdict:** Sound, high confidence. The repair is source-bound,
  independently reviewed, and leaves H2.6b, H2.7, and H3 closed.

### Make H2.3a native-posterior-first under one joint-law owner

- **Choice:** Define the selected H2.3a process from Mathlib's native posterior
  of the actual finite-prefix kernel under the embedded fair H1.3 prior. Build
  one prior-predictive joint law on a static Boolean mean and an i.i.d. real
  Gaussian trajectory; prove the posterior mass is almost everywhere equal to
  the hidden-parameter indicator's conditional expectation under that joint
  law. Do not define a conditional expectation and merely name it posterior.
- **Reason:** `posterior_kernel` readiness proves only that a trivial identity
  posterior is Markov. The scientific martingale semantics require the native
  selected posterior, conditional distribution, filtration, and conditional
  expectation to describe the same joint model. Keeping those connections as
  a.e. theorems preserves Mathlib's disintegration boundary and one posterior
  owner.
- **Verdict:** Sound, high confidence. The actual native posterior,
  conditional distribution, and conditional expectation now compile on the
  same joint law; exact-statement and mutation tests reject a weakened bridge.

### Use the Lévy upward limiting-observation conditional expectation in H2.3a

- **Choice:** State the H2.3a limit as almost-everywhere `Filter.atTop`
  convergence to the conditional expectation under
  `⨆ n, observationFiltration n`, using root
  `MeasureTheory.tendsto_ae_condExp`. Do not apply
  `Integrable.tendsto_ae_condExp` to the hidden-parameter indicator with a
  premise that it is measurable under
  `⨆ n, observationFiltration n`.
- **Reason:** The stronger method-style theorem converges to the indicator only
  after the tail-measurability premise is supplied. For this model that premise
  is the identification/consistency step owned by H2.3b, not evidence available
  to H2.3a. The increasing union is the sigma-algebra generated by all finite
  observations, not the Kolmogorov tail sigma-field. Lévy upward convergence
  supplies the exact unidentified martingale limit without smuggling that
  downstream theorem into the entry slice.
- **Verdict:** Sound, high confidence. The pinned native instantiation
  compiles and its endpoint remains the limiting-observation conditional
  expectation, without a hidden identifiability premise.

### Treat H2.0 posterior readiness as historical entry evidence

- **Choice:** Preserve the accepted H2.0 matrix, probe, and receipt unchanged.
  Record their posterior, conditional-expectation, and martingale-convergence
  rows as API availability evidence only, and close the concrete Gaussian
  joint-law/native-posterior bridge gap in the new H2.3a source and focused
  slice test.
- **Reason:** The accepted probe checks a Boolean identity posterior,
  conditional-expectation integrability, and a convergence theorem carrying a
  terminal measurability premise. Rewriting the historical receipt would erase
  what it actually established; treating it as full H2.3a evidence would be
  fail-open.
- **Verdict:** Sound, high confidence. The historical evidence stays true while
  the active slice owns the missing red-to-green obligation.

### Keep static parameter learning separate from filtering and H1 regression

- **Choice:** Freeze H2.3a on a Boolean mean sampled once, variance-one H2.1
  laws at means zero and one, `Set.Iic n` prefixes containing coordinates
  `0, …, n`, and the prior-predictive joint measure. Re-export H1.3's exact
  `posteriorBadMass_eventually_contracts` theorem through a same-signature
  regression wrapper, without equating Boolean and Gaussian observation or
  trajectory carriers. Import no OU, semigroup, grid-path, or filter owner.
- **Reason:** Parameter-posterior martingales live under the Bayesian joint
  law; H2.6 filtering evolves a latent state under a transition model. Sharing
  the word posterior does not create a carrier or dependency edge. The H1
  theorem is evidence that the accepted discrete lane remains intact, not a
  Gaussian proof or translation layer.
- **Verdict:** Sound, high confidence. Success opens only H2.3b; H2.6 and the
  H2.7 consistency edge remain unchanged.

### Derive the maintained finite-axis Gaussian semigroup from raw precision

- **Choice:** H2.5b stores only finite-axis symmetric positive-definite
  precision and a center. Covariance is the inverse, the transition is a
  measurable state-plus-noise `compProd`/`map` kernel, and the full
  `NNReal`-time weak limit is proved through probability-measure wrappers and
  Lévy characteristic-function convergence. The exact `Fin 1` transport uses
  H2.5a with diffusion variance rate two.
- **Reason:** A measure-valued surrogate, stored covariance/Markov certificate,
  sequence-only limit, or unnamed scalar resemblance would bypass the actual
  ownership and acceptance seams.
- **Verdict:** Sound, high confidence. The kernel, chronology, invariant law,
  moments, weak limit, and exact scalar equality compile warning-free and all
  25 theorem axiom reports are nonempty and standard only.

### Do not infer reversibility from the H2.5b theorem surface

- **Choice:** Describe H2.5b as a symmetric-precision linear-Gaussian
  semigroup and claim the invariant law actually proved. Do not call it a
  native reversible law or detailed-balance result without a separately owned
  theorem.
- **Reason:** Symmetric precision motivates a reversible OU model
  mathematically, but the maintained public roster derives invariance and does
  not expose H2.4b `ReversibleLaw` or a joint-measure symmetry proof.
- **Verdict:** Sound, high confidence. Source comments, slice scope, and exit
  evidence now mechanically exclude the stronger claim.

### Specialize H2.5b through one named four-axis carrier

- **Choice:** Give the preregistered external--sensory--active--internal axes a
  named type and explicit `Fin 4` equivalence; store only the fixed precision
  matrix and center; derive covariance as the inverse; prove the exact
  eigensystem and specialize H2.5b's transition, semigroup, invariant law, and
  weak limit. Identify the normalized all-ones projection exactly with H2.5a
  at rate two and diffusion variance rate two.
- **Reason:** A positional tuple coercion, separately stored covariance, numeric
  diagonalization receipt, or second Gaussian-semigroup owner would weaken the
  carrier and ownership contract. The exact scalar projection prevents the
  four-axis export from being merely shape-adjacent to the accepted scalar
  model.
- **Verdict:** Sound, high confidence. The 18-definition/42-theorem owner
  compiles warning-free; its complete typed export, syntax-independent public
  declaration census, standard-axiom audit, packaging, and independent review
  pass. The result deliberately does not infer conditioning, precision-based
  conditional independence, reversibility, or H3 eligibility.

### Reuse the accepted native posterior in one chronological finite filter

- **Choice:** H2.6a preserves the accepted R0 proof route and adds only one
  fixed-duration `List ℝ` recursion. The empty list returns the prior; for
  `observation :: rest`, the exact posterior update is applied before the
  tail. Native-posterior equality remains evidence almost everywhere, while
  density positivity and zero singleton mass are stated separately.
- **Reason:** A time-varying schedule, pointwise posterior identity, atomic
  evidence interpretation, or second filter owner would widen the selected
  model and erase the disintegration boundary already proved by R0.
- **Verdict:** Sound, high confidence. The 16-theorem maintained owner is
  warning-free, standard-axiom only, byte-projected, and independently
  approved.

### Narrow H2.6b to one-step posterior-predictive quadratic decision risk

- **Choice:** After H2.6a, H2.6b may use a finite action set whose actions
  select existing H2.5a OU parameters at one common positive duration. Define
  terminal squared-error risk from the actual transitioned posterior law plus
  a separately typed nonnegative action penalty, attain it with the existing
  `FEP.ControlledMarkov.finiteArgmin`, and relate the native-posterior selector
  only evidence almost everywhere.
- **Reason:** H1.4's policy-tree model has no transition field, and the live
  tree/decision APIs do not provide a Gaussian reward--EFE equivalence or the
  measurable observation-to-action selector required by Mathlib's global
  `bayesRisk`/`IsBayesEstimator` contract.
- **Verdict:** Sound, high confidence and accepted. The one-structure,
  17-definition/19-theorem owner derives the expected-square identity from the
  actual composed Gaussian law, attains the finite selector, preserves
  evidence-a.e. native-posterior scope, and proves both a strict
  transition-derived Boolean witness and a tie counterexample. Packaging,
  warning-free compilation, nonvacuous standard-axiom probes, and independent
  control/science review pass. Multi-step tree planning, EFE/reward relabeling,
  and generic Bayes-estimator optimality remain no-go or separately gated.

### Identify the H2.3a endpoint through an observation-measurable SLLN recovery

- **Choice:** For the selected Boolean mean model, derive branchwise Gaussian
  strong laws and construct an explicit recovery statistic measurable under
  `⨆ n, observationFiltration n`. Use almost-everywhere equality with the
  hidden-parameter indicator to identify H2.3a's limiting conditional
  expectation. Import `Mathlib.Probability.StrongLaw` directly.
- **Reason:** Applying a conditional-expectation convergence theorem with an
  assumed terminal-measurability premise would store the identification result.
  Calling the finite-observation supremum a Kolmogorov tail field would also
  change the theorem. Direct import ownership prevents the proof from relying
  on an unrelated transitive H1 dependency.
- **Verdict:** Sound, high confidence. The recovery construction compiles under
  the exact observation filtration and fresh statistical review approved the
  identification boundary.

### Export both joint-law and fixed-truth posterior consistency

- **Choice:** Prove consistency first under the selected prior-predictive joint
  law, then extract each positive-prior Boolean branch to obtain convergence
  under the corresponding fixed-truth trajectory law.
- **Reason:** Joint-law almost-everywhere convergence alone can understate the
  scientific claim expected from a two-point identifiable model. Conversely,
  a fixed-truth theorem without explicit positive prior support would hide a
  required branch-extraction premise.
- **Verdict:** Sound, high confidence. Both fair-prior atoms are proved positive,
  and the two consistency modes are separately named and compiler checked.

### Transfer only weak and bounded consequences of H2.3b consistency

- **Choice:** Package the Boolean parameter posterior as a native
  `ProbabilityMeasure`, prove weak convergence to the sampled-parameter Dirac
  law, transfer bounded continuous expectations, and use the genuine bounded
  zero-one Bayes risk `min(p, 1 - p)` in `[0, 1/2]`.
- **Reason:** Weak convergence alone does not transfer entropy, logarithms, or
  arbitrary unbounded losses. A total-variation, rate, or continuous-parameter
  claim would require new mathematics not supplied by the selected model.
- **Verdict:** Sound, high confidence. Each consequence has a distinct theorem,
  and the exit surface explicitly excludes the stronger modes.

### Keep nonidentifiability native and predictive-law almost everywhere

- **Choice:** Use an input-independent native likelihood as the same-law
  countermodel and prove that its native posterior equals the prior only almost
  everywhere under the predictive observation law.
- **Reason:** Native posterior kernels are disintegrations and need not be
  pointwise canonical on null evidence. A project-local posterior surrogate or
  pointwise equality would erase that boundary.
- **Verdict:** Sound, high confidence. The native Markov kernels compile and the
  theorem states the exact almost-everywhere scope.

### Ratchet H2.3b at the Lean environment and typed proposition boundaries

- **Choice:** Pin the complete namespace with `#print prefix`, compile exact
  consumers for all twelve H2.3b theorem types, parse every theorem's quoted or
  unquoted axiom report non-vacuously, and mutation-test hidden declarations,
  extra premises, and forbidden axioms.
- **Reason:** Line-oriented source regexes and quoted-only axiom parsers can pass
  while public lemmas, attributed declarations, stronger premises, or Lean 4.33
  unquoted axiom names escape the acceptance contract.
- **Verdict:** Sound, high confidence. The 64-declaration census and typed
  consumers fail on each adversarial mutation and pass against the accepted
  source without changing the scientific theorem bodies.

### Insert an append-only H2.5d-R0 gate before maintained conditioning

- **Choice:** Preserve the frozen H2.0
  `gaussian_conditioning_precision = blocking_no_go` row and insert a new
  source-bound H2.5d-R0 spike/decision between accepted H2.5c and maintained
  H2.5d. The spike is not manifested, projected, or aggregated; its receipt may
  open only H2.5d implementation.
- **Reason:** Treating the historical blocking row as a dependency that later
  turns green would rewrite accepted evidence. Opening H2.5d from source search
  or matrix algebra would repeat the fail-open readiness error already repaired
  by the H2.5b-R0 and H2.6a-R0 addenda.
- **Verdict:** Sound, high confidence. The DAG now has the explicit edge
  `H2.5c -> H2.5d-R0 -> H2.5d -> H2.7` and leaves the accepted matrix and
  receipt immutable.

### Make native joint reconstruction and CondIndepFun the fatal H2.5d seam

- **Choice:** On `stationaryLaw (0 : StandardizedState)`, derive the exact
  blanket/endpoints `compProd`, identify Mathlib's endpoint and scalar
  conditional distributions blanket-marginal almost everywhere, and prove
  native external--internal `CondIndepFun` given the sensory--active pair. A PDF,
  Schur complement, residual covariance calculation, or custom certificate is
  not terminal evidence.
- **Reason:** Mathlib regular conditional distributions are almost-everywhere
  objects. Precision-zero algebra is scientifically relevant only after an
  exact native joint measure is reconstructed and conditional-kernel uniqueness
  is applied.
- **Verdict:** Sound, high confidence and accepted. The warning-free spike
  reconstructs the exact native joint, identifies pair and scalar conditional
  distributions blanket-marginal almost everywhere, and proves native
  `CondIndepFun`; both independent reviews approved the source-bound result.

### Bound R0 to the centered law and defer exact extensions to maintained H2.5d

- **Choice:** R0 tests only the centered accepted Fin4 stationary law. It uses
  residuals `E - (S + A) / 4` and `I - (S + A) / 4`, each variance `1 / 4`, to
  derive the native product conditional. Maintained H2.5d must translate this
  theorem to arbitrary centers and add one exact endpoint conditional-precision
  perturbation `[[4, 1], [1, 4]]` whose covariance has off-diagonal
  `-1 / 15` and whose coordinates are not independent.
- **Reason:** Center translation is not the fatal pinned-API question, while a
  generic arbitrary-block Gaussian graphical-model theorem is far beyond this
  gate. The named two-dimensional perturbation makes the zero condition
  operative without pretending to prove an arbitrary-precision converse.
- **Verdict:** Sound, high confidence and accepted at R0. The centered theorem
  is native and observational; maintained H2.5d still owes the translated
  theorem and bounded nonindependence witness before H2.7 can open.

### Keep one joint factorization as the conditional-law owner

- **Choice:** Prove the endpoint-pair conditional distribution first, then
  derive the external and internal scalar rows by measurable `fst` and `snd`
  composition. Keep all residual orthogonalization and the Bool-indexed
  two-coordinate Gaussian family private.
- **Reason:** Separate scalar conditioning proofs would duplicate the hard
  measure reconstruction and could drift in orientation or almost-everywhere
  scope. The private residual family reuses the fixed Fin4 carrier and invokes
  Mathlib covariance-zero independence without exporting a second model.
- **Verdict:** Sound, high confidence. The exact environment census remains at
  2 abbreviations, 8 definitions, and 12 theorems while typed consumers verify
  the pair, scalar, and native conditional-independence results.

### State the probability-level covariance boundary

- **Choice:** Package the actual stationary-law covariance
  `cov[external, internal; stationaryLaw 0] = 1 / 24` and its nonzero
  consequence alongside `K external internal = 0` and native conditional
  independence. Use the accepted `Sigma` entries only to discharge that native
  covariance equality.
- **Reason:** A matrix-entry conjunction would be algebraically correct but
  weaker than the scientific boundary: nonzero marginal covariance under the
  actual law does not refute the proved conditional factorization.
- **Verdict:** Sound, high confidence. This stricter proposition was caught in
  code review, compiled warning-free, and was independently approved.

### Translate the accepted centered conditional law without collapsing endpoint centers

- **Choice:** In maintained H2.5d, center the external and internal residuals
  separately, prove their independence from the sensory--active blanket, then
  add back the actual blanket offset and the distinct external/internal center
  coordinates. Derive scalar conditional rows only by mapping the proved pair
  conditional owner.
- **Reason:** A shared arbitrary-center conditional mean would silently be
  correct only at center zero. Reconstructing scalar joints independently
  would duplicate the hard `compProd` and conditional-kernel uniqueness seam.
- **Verdict:** Sound, high confidence and accepted. The arbitrary-center native
  joint, pair/scalar blanket-a.e. conditional laws, and `CondIndepFun` compile
  warning-free and passed two independent reviews.

### Keep the perturbation diagnostic bivariate and probability-level

- **Choice:** Use only the fixed `Fin 2` precision `[[4, 1], [1, 4]]`, define
  covariance as its inverse, and refute native coordinate independence from
  the actual Gaussian-law covariance `-1 / 15` via Mathlib's covariance-zero
  consequence of `IndepFun`.
- **Reason:** This makes the nonzero precision coupling operative without
  inventing a second Fin4 carrier, storing a covariance certificate, or
  claiming a generic precision/conditional-independence converse.
- **Verdict:** Sound, high confidence and accepted. Positive definiteness,
  inverse entries, actual covariance, and native non-independence are all
  derived theorem obligations.

### Record the R0 owner-lifecycle correction explicitly

- **Choice:** Preserve the original accepted R0 test digest, record the exact
  pre-receipt-only owner-absence correction and corrected receipt/test digests
  in an append-only lifecycle provenance file, and forbid another in-place R0
  repair mutation.
- **Reason:** The R0 test correctly required no maintained owner before its
  `go` receipt existed, but that unconditional assertion became false after the
  decision opened H2.5d and the owner landed. Silently rewriting the accepted
  evidence would erase provenance; leaving it unchanged would make every
  legitimate downstream implementation fail.
- **Verdict:** Sound, high confidence after review. The amendment changes no
  spike, H2.0 row, acceptance receipt, probe, theorem, or scientific decision;
  both before/after test hashes remain explicit.

## Needs user

None for the accepted H2 lanes. Any blocking no-go that removes a solid H2.7
clause or changes the cross-horizon DAG requires explicit review before the
affected package can open.
