# P4b report — continuous emission under contract v0.2

Slice: `specs/gnn-bridge-p4b-continuous-emission/` (Direction 1 P4
reopen). Date: 2026-09-04. Spec: [README.md](README.md). fep_lean digest
at run time: `a4bd477165ff` (contract v0.2 commit); GNN checkout:
`64d49355acf1` (fleet-dirty tree, read-only for fep_lean purposes; the
pin surfaces `doc/gnn/gnn_syntax.md` and
`src/pipeline/step_registry.py` were clean at the W1 pin and remain so
at this run).

## P4 no-go supersession note

P4 (`specs/gnn-bridge-p4-continuous-spike/README.md`) closed at the
extraction boundary: `F = exp(-1)` and `Q = 1 - exp(-2)` are
transcendental, and contract v0.1 admitted terminating decimals only.
The user decision to adopt the contract-level float64 rounding extension
reopened the phase; contract v0.2 landed in both checkouts in the same
working session (canonical commit `a4bd477`, mirror edited same
session, uncommitted in GNN per the fleet's commit ownership). The P4
extraction table is reused unchanged; only the `F`/`Q` status changed
(from no-go to float64 emission with exact formulas recorded verbatim
in provenance).

## Artifacts

| Path | State | Role |
| --- | --- | --- |
| `specs/gnn-bridge-p4b-continuous-emission/README.md` | new | Spec (opened before code) |
| `specs/gnn-bridge-p4b-continuous-emission/projection_continuous.py` | new | Deterministic emitter, `--check` freshness mode |
| `specs/gnn-bridge-p4b-continuous-emission/gnn-input/FepLeanContinuousOU.md` | generated | The emitted continuous document |
| `specs/gnn-bridge-p4b-continuous-emission/gnn_output/` | generated | GNN pipeline run artifacts (steps 3, 5, 11, 12) |
| `specs/gnn-bridge-p4b-continuous-emission/REPORT.md` | new | This file |

## Emitted values (contract v0.2 rounding)

| Field | Exact Lean value | Emitted | Policy |
| --- | --- | --- | --- |
| `F` | `exp(-1)` (`scalar_gaussian_semigroup.lean:42-43`) | `0.36787944117144233` | float64 shortest round-trip repr |
| `Q` | `1 - exp(-2)` (`linear_gaussian_semigroup.lean:1217-1233`) | `0.8646647167633873` | float64 shortest round-trip repr |
| `H` | `1` (identity readout, `gaussian_filter.lean:46-49`) | `1.0` | terminating, exact |
| `R` | `1` (`posterior_convergence.lean:38-40`) | `1.0` | terminating, exact |
| `prior_mean` | `0` (`smooth_reference_kernel.lean:66-68`) | `0.0` | terminating, exact |
| `prior_cov` | `1` (proved stationary variance, `smooth_reference_kernel.lean:96-101`) | `1.0` | terminating, exact |

`Signature` carries `exact_formulas: F = exp(-1); Q = 1 - exp(-2)`
verbatim plus the repo/commit/module/instance/`projection_tool`/
`target_syntax`/`rounding_policy` rows.

One emitter correction during bring-up, recorded honestly: the first
emission wrote the 1×1 matrices as single-paren vectors `(v)`; the GNN
side's continuous dimension check
(`GeneralizedNotationNotation/src/gnn/pomdp_extractor.py:881` "F must be
a square matrix") requires the 1×1 matrix shape, and the parser reads
`((v))` as a 1×1 matrix (the Q2/Q3 mirror finding's frozen
`((…),(…))`-shape convention degenerating to one row). Re-emitted with
`((v))` blocks; determinism and freshness re-verified after the fix.

## Commands and exit codes

| Command (fep_lean root unless noted) | Exit | Result |
| --- | --- | --- |
| `uv run ruff check specs/gnn-bridge-p4b-continuous-emission/projection_continuous.py` | 0 | All checks passed |
| `uv run mypy specs/gnn-bridge-p4b-continuous-emission/projection_continuous.py` | 0 | no issues |
| `uv run python specs/gnn-bridge-p4b-continuous-emission/projection_continuous.py` | 0 | wrote `FepLeanContinuousOU.md` (4893 bytes) |
| `uv run python specs/gnn-bridge-p4b-continuous-emission/projection_continuous.py --check` | 0 | FRESH — regenerated bytes match on-disk artifact |
| GNN: `uv run gnn validate <p4b>/gnn-input/FepLeanContinuousOU.md --strict` | 0 | valid (10 variables, 7 connections) |
| GNN: `uv run python src/main.py --target-dir <p4b>/gnn-input --output-dir <p4b>/gnn_output --only-steps "3,5,11,12" --verbose` | 0 | 4 steps, 4 successful, 0 warnings, 0 failed |
| Direct extractor probe (GNN): `POMDPExtractor.extract_from_gnn_content` + `detect_model_kind` | — | `gnn_section: 'FepLeanContinuousOU continuous'`, model kind `CONTINUOUS` |

## Render results (step 11) — honest per-backend statuses

`render_processing_summary.json`:
`successful_framework_renderings = 5/5`, `failed = []`,
`unsupported_framework_renderings` = pymdp, activeinference_jl,
discopy, bnlearn — each with message "continuous-state model:
<framework> supports discrete POMDPs only". Per the contract's
`unsupported` rule these are excluded from the success denominator
(the per-file summary's `frameworks_processed` is exactly the five
continuous-capable backends). `model_kind: "continuous"` detected
mechanically from the `GNNSection` keyword.

| Backend | Status |
| --- | --- |
| jax | rendered (success) |
| pytorch | rendered (success) |
| numpyro | rendered (success) |
| stan | rendered (success) |
| rxinfer | rendered (success) |
| pymdp | `unsupported` (discrete-only) |
| activeinference_jl | `unsupported` (discrete-only) |
| discopy | `unsupported` (discrete-only) |
| bnlearn | `unsupported` (discrete-only) |

## Execution results (step 12)

`12_execute_output/summaries/execution_summary.json`:
5 scripts found, 4 success, 0 failed, 1 skipped.

| Backend | Status | Evidence |
| --- | --- | --- |
| jax | success rc 0 | `all_valid: True` (`means_finite`, `posterior_cov_psd`, `rmse_finite`, `controls_finite`) |
| numpyro | success rc 0 | NUTS run completed |
| rxinfer | success rc 0 | Julia RxInfer.jl wrote `simulation.log`, `simulation_log.json`, belief/free-energy PNGs |
| stan | success rc 0 | CmdStan run completed |
| pytorch | skipped | torch not installed in the GNN venv (`PYTORCH_AVAILABLE = False`, optional dependency) — a skip, not a failure |

## Honest boundary (evidence firewall)

- The GNN pipeline run establishes only that the emitted document
  parses, type-checks, renders on the continuous-capable backends, and
  executes — nothing about Lean semantics (contract section 7).
- The float64 `F`/`Q` emissions are approximations of the exact Lean
  reals by contract v0.2; no executed statistic is promoted to a proved
  property, and the exact formulas live in `Signature` provenance.
- The P4 no-go is superseded only at the rounding-policy level; the
  dynamics-gauge caveat from the Q3 mirror findings (one-step `F` vs
  `exp(-t·precision)` identification) is untouched — this slice emits
  the document, it does not state a carrier identification.
