import Mathlib

/-- Root module for the FepSketches library. Ephemeral topic files under `FepSketches/`
are typechecked via `lake env lean` without being listed as default targets. -/
theorem fepSketchesRoot : True := True.intro
