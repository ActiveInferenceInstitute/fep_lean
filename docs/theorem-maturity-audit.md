# Theorem maturity audit

<!-- AUTO-GENERATED by scripts/theorem_maturity_audit.py; edit config/theorem_maturity.yaml. -->

This audit reviews the semantic reach of each catalogue row beyond
sorry-free compilation. It is deliberately separate from
`mathlib_status`: `real` records that the declared Lean/Mathlib sketch
is a native compilation target, not that the FEP-facing prose has been
fully formalized.

**Review date:** `2026-07-31`<br>
**Native evidence boundary:** The native Lean evidence is the per-topic sweep from uv run fep-lean verify; it establishes compilation only, not the semantic disposition below.

A `scope_gap` or `assumption_gap` is an honest follow-up boundary, not a
compiler failure. No catalogue row was relabelled by this review.

## Status-change policy

A future mathlib_status change requires a refreshed native Lean receipt and an explicit changelog entry; this audit never turns a proxy into a stronger mathematical claim.
A status change must update the catalogue source, regenerate YAML and the
aggregate, rerun the native Lean probe, and retain the receipt in
`CHANGELOG.md`.

## Dispositions

- `proxy`: useful foundational formal fact, with a narrower scope than the prose.
- `conditional_proxy`: meaningful conditional fact whose hypotheses are explicit.
- `structural_proxy`: type, set, metric, or algebra closure fact used as an anchor.
- `scope_gap`: the theorem compiles but omits a named FEP or physical invariant.
- `assumption_gap`: the stated assumptions are vacuous or encode the conclusion.

## Topic review

| ID | Primary theorem | Intended invariant | Assumption review | Non-vacuity review | Acceptance probe | Disposition |
| --- | --- | --- | --- | --- | --- | --- |
| fep-001 | fep001_measure_union_le | Measure mass is subadditive over two events. | Conditional on an arbitrary measurable space and measure; no probability normalization or finite mass is assumed. | Structural: the zero measure inhabits the context, while a nonzero probability witness is deliberately absent. | native Lean compile of fep001_measure_union_le; semantic follow-up is a nonzero finite-measure instantiation. | proxy |
| fep-002 | fep002_elbo_bound | A nonnegative KL remainder makes free energy a lower bound on log evidence. | Conditional: the decomposition and KL nonnegativity are hypotheses; the probabilistic definitions are not formalized here. | Witnessed by logEvidence = freeEnergy = klDiv = 0, with the strict probabilistic witness intentionally deferred. | native Lean compile of fep002_elbo_bound plus a finite real witness satisfying both hypotheses. | conditional_proxy |
| fep-003 | fep003_efe_monotone | Finite expected-cost aggregation preserves pointwise cost order. | Conditional on a finite state set and pointwise order; no policy distribution or expectation normalization is assumed. | Witnessed by a singleton state set and constant costs with ca ≤ cb. | native Lean compile of fep003_efe_monotone and a singleton Finset instantiation. | proxy |
| fep-004 | fep004_inner_self_nonneg | The finite coordinate inner product with itself is nonnegative. | Unconditional over two real coordinates; this is a Euclidean PSD anchor, not a Fisher-information construction. | Witnessed by the vector (1, 0). | native Lean compile of fep004_inner_self_nonneg with zero and nonzero coordinate witnesses. | structural_proxy |
| fep-005 | fep005_total_cover | A total state-to-block assignment places every state in one partition block. | Unconditional for any function Fin 20 → Fin 4; disjointness and totality come from functional assignment. | Witnessed by any constant assignment and by the identity-style finite assignment. | native Lean compile of fep005_total_cover and finite membership checks for a concrete assignment. | structural_proxy |
| fep-006 | fep006_comp_measurable | Measurable state maps remain measurable under composition. | Conditional on measurability of both maps; no dynamics, invariant measure, or flow equation is assumed. | Witnessed by identity and constant maps on standard measurable spaces. | native Lean compile of fep006_comp_measurable with identity-map witnesses. | structural_proxy |
| fep-007 | fep007_message_agg_mono | A nonnegative factor-weighted message aggregate is monotone in incoming messages. | Conditional on nonnegative factors and pointwise incoming-message order; graph normalization and fixed-point semantics are absent. | Witnessed by constant unit factors, zero lower messages, and unit upper messages. | native Lean compile of fep007_message_agg_mono on a singleton neighbor set. | proxy |
| fep-008 | fep008_exists_minG | A real-valued objective attains a minimum on a finite nonempty policy set. | Conditional on finite nonemptiness; the theorem does not define expected free energy or prove a unique policy. | Witnessed by a singleton policy set with any real objective. | native Lean compile of fep008_exists_minG and a singleton-policy witness. | conditional_proxy |
| fep-009 | fep009_likelihood_mono | Measure-based likelihood mass is monotone under event inclusion. | Conditional on an arbitrary measure and set inclusion; the catalogue prose mentions factorization, but this theorem does not establish independence. | Witnessed by nested finite sets under a counting or zero measure. | native Lean compile of fep009_likelihood_mono; semantic follow-up is a product-measure factorization theorem. | scope_gap |
| fep-010 | fep010_detailed_balance | The real exponential of opposite scalar energies multiplies to one. | Unconditional algebra over ℝ; the identity is not a physical detailed-balance law without transition rates or a stationary measure. | Witnessed by every real a, including a = 1. | native Lean compile of fep010_detailed_balance and evaluation at a = 1. | scope_gap |
| fep-011 | fep011_negLog_nonneg_prob | Negative log probability is nonnegative on the interval (0, 1]. | Conditional on strict positivity and an upper bound; no probability space or random variable is introduced. | Witnessed by u = 1/2 and the boundary u = 1. | native Lean compile of fep011_negLog_nonneg_prob with u = 1/2. | conditional_proxy |
| fep-012 | fep012_gibbsPartition_pos | A finite nonempty sum of exponential Boltzmann weights is strictly positive. | Conditional only on policy-set nonemptiness; no normalization, temperature parameter, or entropy objective is proved. | Witnessed by a singleton policy set and arbitrary finite costs. | native Lean compile of fep012_gibbsPartition_pos and a singleton-policy witness. | conditional_proxy |
| fep-013 | fep013_helmholtz_mono_entropy | At positive temperature, increasing entropy decreases U − TS. | Conditional on T > 0 and S₁ ≤ S₂; U, T, and S are unconstrained real scalars rather than thermodynamic observables. | Witnessed by U = 0, T = 1, S₁ = 0, and S₂ = 1. | native Lean compile of fep013_helmholtz_mono_entropy with the stated scalar witness. | structural_proxy |
| fep-014 | fep014_dpi_measurable | Composition of measurable maps is measurable, a prerequisite for pushforward arguments. | Conditional on two measurable maps; despite the title, no KL nonnegativity, chain rule, or data-processing inequality is formalized. | Witnessed by identity maps and constant maps on measurable spaces. | native Lean compile of fep014_dpi_measurable; semantic follow-up is an actual KL data-processing statement. | scope_gap |
| fep-015 | fep015_measurable_comp | Variational objectives remain measurable under composition of measurable maps. | Conditional on measurability of both maps; no integrability or objective value is assumed. | Witnessed by identity and constant maps. | native Lean compile of fep015_measurable_comp with identity-map witnesses. | structural_proxy |
| fep-016 | fep016_precision_weighted | A nonnegative precision-weighted squared residual is nonnegative. | Conditional only on precision ≥ 0; no likelihood, Hessian, or approximation error bound is formalized. | Witnessed by precision 1 and x = μ + 1. | native Lean compile of fep016_precision_weighted and a positive-precision witness. | conditional_proxy |
| fep-017 | fep017_posterior_nonneg | An unnormalized likelihood-times-prior posterior is pointwise nonnegative. | Conditional on pointwise nonnegative factors; normalization and posterior mass-one properties are absent. | Witnessed by constant unit prior and likelihood on a finite state. | native Lean compile of fep017_posterior_nonneg with unit factors. | scope_gap |
| fep-018 | fep018_triangle | Euclidean distance satisfies the triangle inequality. | Unconditional in a two-dimensional Euclidean space; this is not a theorem about statistical-manifold geodesics. | Witnessed by three distinct finite coordinate vectors. | native Lean compile of fep018_triangle and a concrete three-point distance calculation. | scope_gap |
| fep-019 | fep019_mixture_nonneg | A finite weighted sum of nonnegative prior-predictive terms is nonnegative. | Conditional on nonnegative weights and likelihoods over the support; no normalized prior or predictive density law is assumed. | Witnessed by a singleton support with unit weight and likelihood. | native Lean compile of fep019_mixture_nonneg on a singleton support. | conditional_proxy |
| fep-020 | fep020_descent | A scalar step x − η·grad decreases x when η and grad are positive. | Conditional on positive scalar step and gradient; it is not a stochastic Langevin sampler or convergence theorem. | Witnessed by x = 0, η = 1, and grad = 1. | native Lean compile of fep020_descent with the scalar witness. | scope_gap |
| fep-021 | fep021_efe_zero_iff | A sum of two nonnegative EFE components vanishes exactly when both vanish. | Conditional on nonnegative components; the risk/ambiguity-to-epistemic/pragmatic equivalence is merely assumed elsewhere in the sketch. | Witnessed by (e, p) = (0, 0), with positive pairs demonstrating the converse boundary. | native Lean compile of fep021_efe_zero_iff and finite scalar witnesses. | conditional_proxy |
| fep-022 | fep022_pushforward_nonneg | Pushforward measure mass is nonnegative on every measurable-space event. | Conditional on a measurable map parameter, although the measurability hypothesis is not used by this positivity theorem; no posterior predictive statistic is proved. | Witnessed by the zero measure and by identity pushforward on a standard Borel space. | native Lean compile of fep022_pushforward_nonneg; semantic follow-up is a predictive calibration or measure-preservation property. | scope_gap |
| fep-023 | fep023_monotone | Adding available policies can only enlarge the reachable-affordance set. | Conditional on finite-policy inclusion and a deterministic policy-to-sensory map; no distributional reachability is modeled. | Witnessed by nested singleton policy sets and a nonconstant map. | native Lean compile of fep023_monotone and explicit finite-set inclusion. | structural_proxy |
| fep-024 | fep024_log_ratio | Positive real log ratios decompose into a difference of logs. | Conditional on positive numerator and denominator; this is an algebraic KL ingredient, not KL itself. | Witnessed by a = 2 and b = 1. | native Lean compile of fep024_log_ratio with positive scalar witnesses. | conditional_proxy |
| fep-025 | fep025_skew_diag_zero | A skew-symmetric real matrix has zero diagonal. | Conditional on matrix skew symmetry; no probability current, gradient flow, or NESS stationarity is formalized. | Witnessed by the zero 1 × 1 matrix and a nonzero 2 × 2 skew matrix. | native Lean compile of fep025_skew_diag_zero with concrete skew matrices. | scope_gap |
| fep-026 | fep026_complexity_increases | The penalty −log p increases as a positive prior mass decreases. | Conditional on positive p and q with p ≤ q; the q positivity hypothesis is unused by the monotonicity result. | Witnessed by p = 1/2 and q = 1. | native Lean compile of fep026_complexity_increases and a positive ordered pair. | conditional_proxy |
| fep-027 | fep027_prod_univ | The product measure assigns product mass to the full rectangular space. | Conditional on measurable spaces and SigmaFinite ν; this is a product-measure law, not a hierarchical generative-model factorization. | Witnessed by finite counting or Dirac measures on finite measurable spaces. | native Lean compile of fep027_prod_univ and finite-measure instances. | scope_gap |
| fep-028 | fep028_softmax_probs_sum_one | Exponentiated policy scores normalize to one over a nonempty finite policy set. | Conditional on nonempty finite support; the definition permits policies outside the support and does not formalize a policy-selection objective. | Witnessed by a singleton support and arbitrary γ and G. | native Lean compile of fep028_softmax_probs_sum_one and a singleton-support normalization check. | conditional_proxy |
| fep-029 | fep029_secant_ineq | A scalar convex combination lies above the lower endpoint when endpoints are ordered. | Conditional on t ∈ [0,1] and a ≤ b; no convex function or Bregman divergence is present. | Witnessed by a = 0, b = 1, and t = 1/2. | native Lean compile of fep029_secant_ineq with an interior interpolation parameter. | scope_gap |
| fep-030 | fep030_entropy_eq_log | The Shannon-form expression for a finite uniform distribution equals log n. | Conditional on n > 0 and a fixed uniform distribution; this proves no maximum over competing distributions. | Witnessed by n = 1 and n = 2. | native Lean compile of fep030_entropy_eq_log with n = 2; semantic follow-up is the maximizing inequality. | scope_gap |
| fep-031 | fep031_partition_pos | A finite nonempty sum of Gibbs weights is strictly positive. | Conditional on finite nonempty support; the separate gibbs_sum theorem is reflexive and does not define normalized probabilities. | Witnessed by a singleton support and any real β and energy. | native Lean compile of fep031_partition_pos and a singleton partition witness. | scope_gap |
| fep-032 | fep032_descent_contracts | The squared scalar gradient step contracts for a step size in (0,1]. | Conditional on η ∈ (0,1]; no objective, gradient regularity, or convergence over iterations is assumed. | Witnessed by x₀ = 1 and η = 1/2. | native Lean compile of fep032_descent_contracts and the stated scalar witness. | conditional_proxy |
| fep-033 | fep033_horizon_mono | Cumulative nonnegative cost is monotone under finite horizon inclusion. | Conditional on finite-set inclusion and nonnegative costs; no temporal transition or discount normalization is assumed. | Witnessed by empty S, singleton T, and constant unit cost. | native Lean compile of fep033_horizon_mono on nested finite horizons. | conditional_proxy |
| fep-034 | fep034_update_nonneg | A categorical likelihood times a nonnegative transition-weighted prior remains nonnegative. | Conditional on pointwise nonnegative factors; transition normalization and posterior normalization are absent. | Witnessed by unit prior, transition, and likelihood on a singleton state set. | native Lean compile of fep034_update_nonneg with unit finite-state factors. | scope_gap |
| fep-035 | fep035_log_mul | Positive products become sums under the real logarithm. | Conditional on positive factors; despite the title, no Jensen inequality or concavity theorem is formalized. | Witnessed by a = 2 and b = 3. | native Lean compile of fep035_log_mul with positive scalar witnesses; semantic follow-up is Jensen proper. | scope_gap |
| fep-036 | fep036_scaledMass_nonneg | A nonnegative real weight times measure mass converted to ℝ remains nonnegative. | Conditional on c ≥ 0; the empirical-Bayes coupling and estimator semantics are not defined. | Witnessed by c = 1 and a finite or zero measure. | native Lean compile of fep036_scaledMass_nonneg and a finite-measure witness. | scope_gap |
| fep-037 | fep037_einstein_pos | The defined product kT × fluctuation is positive when both factors are positive. | Conditional on positive scalars; the fluctuation-dissipation relation is introduced as a definition rather than derived. | Witnessed by kT = 1 and fluctuation = 1. | native Lean compile of fep037_einstein_pos with positive scalar witnesses. | scope_gap |
| fep-038 | fep038_fisher_inner_nonneg | The two-coordinate sum-of-squares inner product is PSD. | Unconditional for finite real coordinates; no Fisher matrix, parameterization, or natural-gradient update is defined. | Witnessed by g = (1, 0) and g = (0, 0). | native Lean compile of fep038_fisher_inner_nonneg with nonzero and zero vectors. | scope_gap |
| fep-039 | fep039_global_mono | A finite sum of local free-energy terms preserves pointwise order. | Conditional on pointwise order over Fin 4; local terms are arbitrary real values and not tied to a partition measure. | Witnessed by constant zero and unit local contributions. | native Lean compile of fep039_global_mono with constant finite functions. | proxy |
| fep-040 | fep040_entropy_mono | The logarithm of a positive variance proxy is monotone in variance. | Conditional on σ₁ > 0 and σ₁ ≤ σ₂; the Gaussian entropy formula and heat-capacity derivative are not formalized. | Witnessed by σ₁ = 1 and σ₂ = 2. | native Lean compile of fep040_entropy_mono and positive ordered variances. | scope_gap |
| fep-041 | fep041_epistemic_nonneg | A weighted finite sum of nonnegative divergence scores is nonnegative. | Conditional on nonnegative weights and scores; `div` is an arbitrary scalar function, not a proved divergence. | Witnessed by a singleton observation with unit weight and divergence. | native Lean compile of fep041_epistemic_nonneg on a singleton observation set. | scope_gap |
| fep-042 | fep042_preimage_mono | A statistic map preserves event inclusion under preimage. | Unconditional for any function; no factorization criterion or sufficiency relation is stated. | Witnessed by identity and constant maps with nested codomain sets. | native Lean compile of fep042_preimage_mono and explicit nested sets. | scope_gap |
| fep-043 | fep043_min_unique_value | Any two global minimizers of a real function have equal value. | Conditional on two explicit global-minimum hypotheses; differentiability and a Hessian condition are not used. | Witnessed by f(x) = x² with x₀ = y₀ = 0, and by any constant function. | native Lean compile of fep043_min_unique_value with a concrete quadratic witness. | conditional_proxy |
| fep-044 | fep044_convex_combo_nonneg | A convex combination of nonnegative endpoint values is nonnegative. | Conditional on α ∈ [0,1] and nonnegative a,b; no α-divergence formula or divergence axiom is present. | Witnessed by α = 1/2 and a = b = 1. | native Lean compile of fep044_convex_combo_nonneg with an interior α witness. | scope_gap |
| fep-045 | fep045_cons_update | A list fold applies one conjugate-family update before folding the tail. | Conditional only on an arbitrary structure with an arbitrary update function; existence is constructive but conjugacy is not constrained. | Witnessed by any family and a singleton or two-element data list. | native Lean compile of fep045_cons_update with a concrete update function. | scope_gap |
| fep-046 | fep046_strict_decrease | A retained stick mass strictly decreases for positive mass and a break fraction in (0,1]. | Conditional on positive u and 0 < v ≤ 1; no simplex normalization across multiple breaks is assumed. | Witnessed by u = 1 and v = 1/2. | native Lean compile of fep046_strict_decrease with the scalar witness. | conditional_proxy |
| fep-047 | fep047_forward_mono | A finite sum-product forward message is monotone in incoming messages. | Conditional on nonnegative factors and pointwise incoming order; no normalized belief propagation semantics are formalized. | Witnessed by a singleton neighbor set, unit factor, and ordered zero/unit inputs. | native Lean compile of fep047_forward_mono on a singleton neighbor set. | proxy |
| fep-048 | fep048_contraction_unique | Two fixed points cannot satisfy a strict distance contraction at those same fixed points. | The strict inequality hypothesis contradicts the fixed-point equalities when x and y are distinct, so the conclusion is logically valid but assumption-vacuous for the intended uniqueness claim. | Only the reflexive case x = y is compatible; a genuine contraction map theorem needs a global Lipschitz hypothesis. | native Lean compile of fep048_contraction_unique plus a review check that the hypotheses reduce to \|x−y\| < \|x−y\|. | assumption_gap |
| fep-049 | fep049_entropy_production_nonneg | The scalar product of nonnegative flux and force is nonnegative. | Conditional on the signs that encode the desired conclusion; no constitutive law or thermodynamic entropy balance is proved. | Witnessed by J = 1 and F = 1, plus zero-flux boundary behavior. | native Lean compile of fep049_entropy_production_nonneg with positive scalar witnesses. | scope_gap |
| fep-050 | fep050_excess_work_nonneg | Work above the defined kT log 2 threshold has nonnegative excess. | Conditional on the threshold inequality; the Landauer erasure model and entropy-change derivation are not formalized. | Witnessed by kT = 1 and W = log 2. | native Lean compile of fep050_excess_work_nonneg and the threshold equality witness. | scope_gap |

## Source links

- [Maintained audit data](../config/theorem_maturity.yaml)
- [Catalogue metadata](../config/topics.yaml)
- [Lean body source](../scripts/catalogue_sketches.py)
- [Native verification command](cli-reference.md)
